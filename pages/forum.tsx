import React, { ReactElement } from 'react'

interface Props {
    
}

function Forum({}: Props): ReactElement {
    return (
        <div>
            Forum
        </div>
    )
}

export default Forum
