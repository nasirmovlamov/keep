import React, { ReactElement } from 'react'

interface Props {
    
}

function Store({}: Props): ReactElement {
    return (
        <div>
            Store
        </div>
    )
}

export default Store
